{
  "name": "test-real-project",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test 'tests/**/*.test.ts'"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
